/**
 * Twitter card image for /essay.
 *
 * Re-exports the Open Graph composition so the Twitter preview stays
 * byte-for-byte aligned with the canonical essay share image.
 */
export { default } from "./opengraph-image";

export const runtime = "nodejs";
export const alt = "Consulting the Dead, Not Distilling the Living — Consult The Dead";
export const size = { width: 1200, height: 630 };
export const contentType = "image/png";
